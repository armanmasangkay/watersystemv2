name: "🚀 Auto deployment"

on: 
  push: 
    branches:
      - main

jobs:
  FTP-Deploy-Action:
    name: "🎉 Deployment actions"
    runs-on: ubuntu-latest
    steps: 
      - name: "🚚 Checkout code"
        uses: actions/checkout@v2

      - name: "📂 Deploying to dev server..."
        uses: appleboy/ssh-action@master
        with:
          username: "macroho1"
          host: "ftp.macrohonwater.ph"
          key: ${{secrets.SSH_KEY}}
          port: 21
          script: |
            cd /home/macroho1/stg-mws.macrohonwater.ph
            git pull origin main
            git status
            composer install
