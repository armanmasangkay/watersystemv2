on:
  push:
    branches:
      - main

name: Macrohon Water System
jobs:
  web-deploy:
    name: mws_app
    runs-on: ubuntu-latest
    steps:
      - name: Get latest code
        uses: actions/checkout@v2.3.2
      - name: Sync files
        uses: SamKirkland/FTP-Deploy-Actions@4.0.0
        with:
          server: ftp.macrohonwater.ph
          username: macrohonwaters@water.macrohonwater.ph
          password: macrohonwaters@2023
          server-dir: /public_html/water.macrohonwater.ph
